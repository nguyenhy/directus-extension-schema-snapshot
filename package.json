{
  "name": "schema-snapshot",
  "version": "0.1.0",
  "description": "Directus schema snapshot normalize + diff toolset",
  "type": "commonjs",
  "bin": {
    "schema-snapshot": "./src/cli/index.js"
  },
  "main": "./src/core/normalize.js",
  "scripts": {
    "test": "node --test test/"
  },
  "license": "MIT",
  "dependencies": {
    "commander": "15.0.0",
    "dotenv": "17.4.2"
  }
}
